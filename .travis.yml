language: d
d: dmd

os:
  - linux

before_install:
  - wget https://apt.llvm.org/llvm.sh && chmod +x llvm.sh && sudo ./llvm.sh 10

addons:
  apt:
    update: true

#script: sh build.sh
script: dub build
