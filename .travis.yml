language: d
d: dmd

os:
  - linux
  #- osx => fix the llvm install command
  #- windows => doesn't support D yet

before_install:
  - wget https://apt.llvm.org/llvm.sh && chmod +x llvm.sh && sudo ./llvm.sh 10

#script: sh build.sh
script: dub build
